[package]
name = "openmd-storage"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "openMD vault: Markdown files on disk plus a SQLite FTS index"

[dependencies]
openmd-core = { path = "../core" }
rusqlite = { version = "0.32", features = ["bundled"] }
walkdir = "2"
thiserror = "2"
