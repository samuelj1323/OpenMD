[package]
name = "openmd-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Block document model, editing ops, and GFM Markdown I/O for openMD"

[dependencies]
pulldown-cmark = "0.13"
ulid = "1"
